/**
 * Static application metadata used across the UI shell.
 * Centralized so the splash/header can be updated in one place.
 */
export const HEADER = {
  title: "PIXELMON",
  tagline: "Aurelia Region — an offline-first monster-catching RPG",
  version: "0.1.0",
} as const;
